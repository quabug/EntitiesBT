using Blob;
using EntitiesBT.Entities;
using NUnit.Framework;
using Nuwa.Blob;
using UnityEditor;

public class TestNodeBlob
{
    [Test]
    public void should_create_node_blob()
    {
        // AssetDatabase.
        // var builder = new DynamicBlobDataBuilder();
        // using var stream = new BlobMemoryStream();
        // new StructBuilder<NodeBlob>();
        // var blobBuilder = new BlobBuilder(Allocator.Temp, UnsafeUtility.SizeOf(NodeType));
        // try
        // {
        //     var dataPtr = blobBuilder.ConstructRootPtrByType(NodeType);
        //     Build(dataPtr, blobBuilder, builders);
        //     return blobBuilder.CreateReferenceByType(NodeType);
        // }
        // finally
        // {
        //     blobBuilder.Dispose();
        // }
    }
}
